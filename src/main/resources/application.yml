spring:
  datasource:
    url: jdbc:mysql://residentia-db.cp2s0wi2ykmu.ap-south-1.rds.amazonaws.com:3306/residentia_db
    username: admin
    password: Residentia25

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  mvc:
    cors:
      allowed-origins: http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:5174,http://127.0.0.1:5175
      allowed-methods: GET,POST,PUT,DELETE,OPTIONS,PATCH
      allowed-headers: "*"
      allow-credentials: true
      max-age: 3600

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 50MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: hspatil222002@gmail.com
    password: wrbhlpyrnefljvig
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  port: ${PORT:8888}
  servlet:
    context-path: /

jwt:
  secret: my-super-secret-key-for-jwt-token-generation-residentia-application-2024-secure-512-bits-minimum-requirement
  expiration: 86400000

# Cloudinary Configuration (Optional)
# If not configured, the application will fall back to local file storage
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:doux976kl}
  api-key: ${CLOUDINARY_API_KEY:553679296573887}
  api-secret: ${CLOUDINARY_API_SECRET:xdyGM0V8Q6jBkx1I3ZNlZVoZRCc}

logging:
  level:
    com.residentia: DEBUG
    org.springframework.web: INFO
    org.springframework.security: DEBUG
  dotnet:
    enabled: false  # Set to true to enable .NET logging service integration
    url: http://localhost:5000
